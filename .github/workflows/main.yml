name: simpleDemo

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Test1 
        run: echo "Running Test"
      - name: Test2
        run: echo "Re-Testing"

  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps: 
      - name: Montinor App
        run: echo "Monitoring App"
      - name: Tracking App
        run: echo "Tracking Unknown activities"
    
